# One target per question. Uncomment as each question is transcribed after the
# test; q01 ships as a copy of prep/templates/q00_template.cpp so it builds now.
add_executable(tt26_q01 q01.cpp)
# add_executable(tt26_q02 q02.cpp)
